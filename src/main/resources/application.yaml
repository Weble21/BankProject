server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bank?useSSL=false&serverTimezone=UTC
    username: root
    password: 245ehdrjs!W
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # 처음엔 update 추천
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret-key: "lecture"
  time: 30

oauth2:
  providers:
    google:
      client-id: ""
      client-secret: ""
      redirect-uri: ""
    github:
      client-id: ""
      client-secret: ""
      redirect-uri: ""
